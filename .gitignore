node_modules
dist
dist-ssr
*.local
.vscode
.DS_Store

# Variables de entorno con credenciales - NUNCA subir al repositorio
.env
.env.local
.env.development
.env.production
.env.staging

# Mantener solo el archivo de ejemplo como referencia
# .env.example  <-- este SI se puede subir (sin valores reales)
